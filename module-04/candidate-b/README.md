# Notes API

A simple REST API for managing notes with SQLite persistence.

## Setup

```bash
pip install fastapi pydantic uvicorn
```

## Run

```bash
python3 api.py
```

The API will start on `http://localhost:8000`. Use `http://localhost:8000/docs` to explore with Swagger UI.
